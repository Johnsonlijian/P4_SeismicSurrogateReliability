# P4 Seismic Surrogate Reliability

Public reproducibility package for the manuscript **False-safe reliability filtering for seismic surrogate models under event-level distribution shift**.

This repository contains code, derived residual traces, derived tables, generated figures, citation metadata and run instructions. It intentionally excludes active submission manuscripts, cover letters, reviewer drafts, internal rounds/logs, credentials and raw third-party data whose redistribution rights are unclear.

## Main outputs

- `outputs/figures/high_target/fig_r29_mechanism_gate_figure1.png`: research-object and reliability-filter mechanism figure.
- `outputs/figures/high_target/Figure_2_metric_ranks.png`: metric-dependent rank comparison.
- `outputs/figures/high_target/Figure_3_residual_mechanism.png`: residual mechanism figure.
- `outputs/figures/high_target/Figure_4_decision_risk_sensitivity.png`: threshold-by-cost decision risk surface.
- `outputs/figures/high_target/fig_r28_true_budget_gate_sensitivity.png`: true event-disjoint Table 7/Table 8 sensitivity figure.
- `outputs/figures/high_target/fig_r30_conditional_coverage_pga_bins.png`: Table 9/Figure 8 intensity-binned coverage diagnostic.

## Rebuild

Use Python 3.10+ with `numpy`, `pandas` and `matplotlib`.

```bash
python scripts/rebuild_residual_mechanism_figure.py
python scripts/rebuild_decision_risk_sensitivity.py
```

## Data boundary

The CSV files in `outputs/high_target/` are derived residual traces and derived decision-risk tables used to reproduce the figures. Raw third-party ground-motion records and source simulation inputs are not redistributed.

## R24 reliability-index extension

The repository now includes the R24 Structural Safety reproducibility increment: derived protocol/model/metric tables, a false-safe reliability-index figure, and verified reference metadata notes. Active submission manuscripts and PDFs remain excluded.


## Repository scope

This repository contains reproducibility materials only: code, configuration, derived tables, generated figures, runbooks, source registries, and citation metadata needed to reproduce the reported computational results. It intentionally excludes active manuscript drafts, journal source files, cover letters, private review rounds, raw third-party archives, credentials, and materials whose redistribution rights are unclear.

The R28 large-budget event-disjoint prediction export contains 796,080 regenerated prediction rows locally; because of size and redistribution boundaries, the public repository provides the generation scripts, aggregate summaries, derived figures, and reproducibility runbook rather than using the repository as an active submission manuscript archive.


## R30/R31 Structural Safety sprint outputs

The final sprint adds derived diagnostics used for the Structural Safety submission package while keeping unpublished manuscripts and submission letters out of the public repository.

Reproduce the main derived increments with:

```bash
python code/67_r28_gate_large_budget_sensitivity.py
python code/68_r29_rebuild_figure1_mechanism.py
python code/69_r30_conditional_coverage_intensity.py
python code/70_r30_polish_conditional_coverage_figure.py
```

Relevant derived outputs:

- Table 7 and Table 8 are generated by `code/67_r28_gate_large_budget_sensitivity.py` from regenerated event-disjoint large-budget prediction exports.
- Table 9 and Figure 8 are generated by `code/69_r30_conditional_coverage_intensity.py` and polished by `code/70_r30_polish_conditional_coverage_figure.py`.
- Figure 1 is generated by `code/68_r29_rebuild_figure1_mechanism.py`; the R31 cleanup removed a literal `\n` rendering artefact in the eligibility-filter box.

Evidence boundary:

The repository contains derived reproducibility materials only. The nonlinear labels are numerical MDOF response outputs from the project benchmark, not field-measured structural damage observations. Raw third-party ground-motion archives, active manuscript PDFs, cover letters, internal rounds and submission packages are intentionally excluded.
